import { WorkspacePlaceholder } from "@/features/workspace-shell/workspace-placeholder";

export default function AdminPage() {
  return (
    <WorkspacePlaceholder
      eyebrow="Admin"
      title="Trung tâm vận hành UpNext"
      description="Nền layout Admin đã sẵn sàng để dựng dashboard, kiểm duyệt, phân quyền động, SEO, Sales CRM và audit log bằng shadcn UI đã custom theo UpNext."
    />
  );
}
